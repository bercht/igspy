<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Login - IgSpy" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-[#0F1419]">
    <!-- Flash Messages -->
    <% if notice %>
      <div class="fixed top-4 right-4 z-50 animate-slide-in">
        <div class="bg-white/[0.05] backdrop-blur-xl border border-[#00D98E]/30 text-white px-6 py-4 rounded-xl shadow-lg flex items-center space-x-3">
          <svg class="w-5 h-5 text-[#00D98E]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <span class="text-sm"><%= notice %></span>
        </div>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="fixed top-4 right-4 z-50 animate-slide-in">
        <div class="bg-white/[0.05] backdrop-blur-xl border border-red-500/30 text-white px-6 py-4 rounded-xl shadow-lg flex items-center space-x-3">
          <svg class="w-5 h-5 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <span class="text-sm"><%= alert %></span>
        </div>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>

<style>
@keyframes slide-in {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

.animate-slide-in {
  animation: slide-in 0.3s ease-out;
}
</style>
